# MCPIP V2 — development / test dependencies (NOT installed into the prod image).
#   Install with:  ./.venv/bin/pip install -r requirements.txt -r requirements-dev.txt
# The adversarial /v1/authorize suite (tests/test_authorize_api.py) drives the app via
# Starlette's TestClient (httpx, shipped transitively by fastapi) and pytest.
-r requirements.txt
pytest>=8.3,<9
mypy==1.13.0
# Release tooling (build-time only — NEVER in requirements.txt or the image):
build>=1.2,<2
cyclonedx-bom>=7.3.1,<8
