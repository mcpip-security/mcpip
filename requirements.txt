pydantic>=2.6,<3
redis>=5.0,<6
PyJWT>=2.8,<3
cryptography>=42.0
# Async HTTP client for the SSRF-guarded, off-hot-path OUTBOUND channels imported at
# boot (auth/jwks_refresher, services/authn_channel, external_pdp, license_refresh,
# telemetry). It is NOT pulled in transitively by fastapi/starlette at runtime, so it
# must be a first-class runtime dependency — without it the gateway fails to import.
httpx>=0.28.1,<1
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
pydantic-settings>=2.2,<3
prometheus-client>=0.20,<1
