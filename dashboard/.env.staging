# Staging / test edition — compiles in the non-production surfaces (e.g. Canary
# Lab, Pipeline Replay) for pre-release validation. Built with
# `npm run build:staging` (vite --mode staging). The environment badge marks a
# staging console unmistakably so it can never be mistaken for production.
VITE_MCPIP_EDITION=staging
