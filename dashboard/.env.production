# Production edition — the lean, shipped console. Loaded by `vite build`
# (mode=production is the default). Sections whose manifest `editions` omit
# `production` are compiled OUT of this bundle entirely (tree-shaken), not merely
# hidden — the strongest guarantee for a security surface.
VITE_MCPIP_EDITION=production
