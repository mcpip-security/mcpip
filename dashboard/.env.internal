# Internal edition — the widest surface, for developers and dogfooding. Same
# non-production sections as staging today; kept as a distinct edition so
# internal-only experiments can diverge from staging without touching the
# production build. Built with `npm run build:internal` (vite --mode internal).
VITE_MCPIP_EDITION=internal
