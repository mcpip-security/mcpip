{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set: the main window renders the bundled operator frontend and reaches the gateway over HTTPS/WSS. No shell, filesystem, process, or dialog access is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
